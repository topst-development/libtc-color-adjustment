AUTOMAKE_OPTIONS = foreign

SUBDIRS = src \
		  include \
		  test

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = tc-color-adjustment.pc

DISTCLEANFILES = tc-color-adjustment.pc
EXTRA_DIST = tc-color-adjustment.pc.in
